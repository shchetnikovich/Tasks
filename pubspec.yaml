name: tasks
description: My task list app.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.1.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"


dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  path_provider: ^2.0.8  
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.12
  google_fonts: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true